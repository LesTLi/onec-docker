name: Pre-build checks

on:
  push:
    branches: [ feature/koronatech]
  pull_request:
    branches: [ feature/koronatech]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        run: echo "Success"
