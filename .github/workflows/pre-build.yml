name: Pre-build checks

on:
  push:
    branches: [ debian/bookworm-slim]
  pull_request:
    branches: [ debian/bookworm-slim]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        run: echo "Success"
