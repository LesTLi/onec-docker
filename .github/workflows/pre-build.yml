name: Pre-build checks

on:
  push:
    branches: [ test-build]
  pull_request:
    branches: [ test-build]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        run: echo "Success"
